<%= form_with(model: component_in_machine, local: true) do |form| %>
  <% if component_in_machine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(component_in_machine.errors.count, "error") %> prohibited this component_in_machine from being saved:</h2>

      <ul>
        <% component_in_machine.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :component_id %>
    <%= form.text_field :component_id %>
  </div>

  <div class="field">
    <%= form.label :machine_id %>
    <%= form.text_field :machine_id %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
