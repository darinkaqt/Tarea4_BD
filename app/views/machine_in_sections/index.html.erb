<p id="notice"><%= notice %></p>

<h1>Machine In Sections</h1>

<table>
  <thead>
    <tr>
      <th>Machine</th>
      <th>Section</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @machine_in_sections.each do |machine_in_section| %>
      <tr>
        <td><%= machine_in_section.machine_id %></td>
        <td><%= machine_in_section.section_id %></td>
        <td><%= link_to 'Show', machine_in_section %></td>
        <td><%= link_to 'Edit', edit_machine_in_section_path(machine_in_section) %></td>
        <td><%= link_to 'Destroy', machine_in_section, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Machine In Section', new_machine_in_section_path %>
