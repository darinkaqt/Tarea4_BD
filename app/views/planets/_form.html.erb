<%= form_with(model: planet, local: true) do |form| %>
  <% if planet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(planet.errors.count, "error") %> prohibited this planet from being saved:</h2>

      <ul>
        <% planet.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name_local %>
    <%= form.text_field :name_local %>
  </div>

  <div class="field">
    <%= form.label :faction_id %>
    <%= form.text_field :faction_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
